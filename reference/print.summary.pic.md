# Print a `summary.pic` object.

Print a `summary.pic` object.

## Usage

``` r
# S3 method for class 'summary.pic'
print(x, digits = 4L, ...)
```

## Arguments

- x:

  A `summary.pic` object.

- digits:

  Number of significant digits for the printed values.

- ...:

  Unused; present for S3 method consistency.

## Value

Invisibly returns `x`.
