# S3 methods for fitted pic objects.

The fitted object has class `c("pic.<family>", "pic")` — methods
dispatch on the second class for shared behavior and on the first class
for family-specific overrides.
