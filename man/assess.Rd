% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess.R
\name{assess}
\alias{assess}
\alias{assess.pic}
\title{Assess performance of a \code{pic} fit.}
\usage{
assess(object, ...)

\method{assess}{pic}(object, newx, newy, true_features = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{pic} object.}

\item{...}{Unused; present for S3 method consistency.}

\item{newx}{Numeric design matrix at which predictions are
evaluated, with the same columns as the training data.}

\item{newy}{Response on the new observations. Numeric vector for
all families except Cox; for Cox, a two-column matrix
\verb{(time, event)} matching the training response format.}

\item{true_features}{Optional integer or character vector listing
the indices (or names) of the true active variables. When
supplied, support-recovery metrics are appended.}
}
\value{
A two-column \code{data.frame} with columns \code{metric} (character)
and \code{value} (numeric).
}
\description{
Given a test set, reports a small set of family-appropriate
predictive metrics. Optionally appends support-recovery diagnostics
when the true active set is known.
}
\details{
The metrics depend on the family:
\describe{
\item{\code{"gaussian"}}{MSE, MAE, R-squared.}
\item{\code{"binomial"}}{accuracy, AUC, binomial deviance.}
\item{\code{"poisson"}}{MSE, MAE, Poisson deviance.}
\item{\code{"exponential"}}{MSE, MAE, Exponential deviance.}
\item{\code{"gumbel"}}{MSE, MAE, deviance computed from the per-sample
negative log-likelihood with a moment estimate of the scale
parameter \eqn{\hat\sigma = \mathrm{sd}(y - \hat\eta)\sqrt{6}/\pi}.}
\item{\code{"cox"}}{Harrell's C-index and the Breslow partial
log-likelihood (negative, normalized by \code{n}).}
}

When \code{true_features} is non-\code{NULL}, four support-recovery metrics
are appended (independent of \code{newx} / \code{newy}):
\code{exact_recovery}, \code{tpr} (true-positive rate / sensitivity), \code{fdr}
(false-discovery rate) and \code{f1} (harmonic mean of precision and
recall). Names are accepted in addition to integer positions when
the fit carries column names.
}
\examples{
data(QuickStartExample)
X <- QuickStartExample$X
y <- QuickStartExample$y
fit <- pic(X, y, family = "gaussian", penalty = "scad")
assess(fit, X, y, true_features = paste0("gene_", 1:5))
}
