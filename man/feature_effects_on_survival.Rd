% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{feature_effects_on_survival}
\alias{feature_effects_on_survival}
\title{Effect of one feature on the Cox survival curve.}
\usage{
feature_effects_on_survival(object, idx, values = NULL)
}
\arguments{
\item{object}{A fitted \code{pic.cox} object.}

\item{idx}{Index of the feature to vary. Either an integer column
position in the training \code{X} or, if \code{X} carried column names, the
variable name. The feature must lie in the model's selected
support; otherwise the curve would be flat in \code{v} and the call
is rejected.}

\item{values}{Optional numeric vector of values to evaluate. When
\code{NULL} (default), the cached grid described in \emph{Details} is
used.}
}
\value{
A list with components \code{time} (length \code{K}) and \code{survival}
(matrix \verb{K x length(values)}, one column per evaluated value).
Column names of \code{survival} are formatted as
\code{"<feature_name> = <value>"} and are picked up automatically by
\code{\link[=plot_survival_curves]{plot_survival_curves()}} for the legend.
}
\description{
Builds the family of survival curves obtained by varying a single
covariate while holding the others at their column means. The
returned object has the same shape as \code{\link[=predict_survival_function]{predict_survival_function()}},
so it composes directly with \code{\link[=plot_survival_curves]{plot_survival_curves()}}.
}
\details{
Both the per-column mean row and a default grid of representative
values (used when \code{values = NULL}) are cached on the fit by \code{pic()}
under \code{attr(fit, "preproc")}, so the training design matrix does
not need to be passed back in. The cached default grid uses the
unique values of the column when there are at most five of them
(handy for ordinal / categorical covariates), and the four
equispaced empirical quantiles (\code{0}, \code{1/3}, \code{2/3}, \code{1}) otherwise.
}
\seealso{
\code{\link[=predict_survival_function]{predict_survival_function()}}, \code{\link[=plot_survival_curves]{plot_survival_curves()}}.
}
