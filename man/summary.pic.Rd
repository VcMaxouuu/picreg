% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-picr.R
\name{summary.pic}
\alias{summary.pic}
\title{Summarize a fitted pic model.}
\usage{
\method{summary}{pic}(object, standardized = FALSE, ...)
}
\arguments{
\item{object}{A fitted \code{pic} object.}

\item{standardized}{Logical; if \code{TRUE}, summarize the standardized
coefficients used internally. Default \code{FALSE} (original scale of \code{X}).}

\item{...}{Unused; present for S3 method consistency.}
}
\value{
An object of class \code{"summary.pic"}: a list with elements
\code{family}, \code{penalty}, \code{lambda}, \code{n_samples}, \code{n_features}, \code{df},
\code{intercept}, \code{standardized}, and \code{coefficients} (a two-column
\code{data.frame} of the non-zero coefficients).
}
\description{
Returns a structured summary of the fit: the family, penalty, selected
\eqn{\hat\lambda}, the problem dimensions, the number of selected
variables, the intercept, and a table of the \strong{non-zero} coefficients
ordered by decreasing absolute value. Coefficients are returned on the
original scale of \code{X} by default (pass \code{standardized = TRUE} for the
internal standardized scale, matching \code{fit$beta}).
}
\examples{
data(QuickStartExample)
fit <- pic(QuickStartExample$X, QuickStartExample$y)
summary(fit)
}
