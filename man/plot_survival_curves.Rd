% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_survival_curves}
\alias{plot_survival_curves}
\title{Plot subject-specific Cox survival curves.}
\usage{
plot_survival_curves(
  sf,
  subjects = NULL,
  max_subjects = 10L,
  labels = NULL,
  n_marks = 8L,
  main = "Individual survival curves",
  ...
)
}
\arguments{
\item{sf}{A list as returned by \code{\link[=predict_survival_function]{predict_survival_function()}} or
\code{\link[=feature_effects_on_survival]{feature_effects_on_survival()}}, with components \code{time} (length
\code{K}) and \code{survival} (matrix \verb{K x m}, one column per curve).}

\item{subjects}{Optional integer vector selecting which columns of
\code{sf$survival} to plot. Defaults to all curves, capped to the
first \code{max_subjects} for legibility.}

\item{max_subjects}{Maximum number of curves drawn when \code{subjects}
is \code{NULL}. Default 10.}

\item{labels}{Optional character vector of labels used in the
legend, one per plotted curve. Defaults to the column names of
\code{sf$survival} when set, otherwise \code{"subject 1"}, \code{"subject 2"},
etc.}

\item{n_marks}{Number of marker glyphs overlaid on each curve to
help distinguish them. Default \code{8}. Set to \code{0} to disable.}

\item{main}{Plot title.}

\item{...}{Additional graphical parameters forwarded to
\code{\link[graphics:matplot]{graphics::matplot()}}.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Step-line visualisation of the output of
\code{\link[=predict_survival_function]{predict_survival_function()}} or \code{\link[=feature_effects_on_survival]{feature_effects_on_survival()}}:
one survival curve per subject (or per feature value) on a common
time grid. To keep curves distinguishable, each curve is drawn with
its own line type and a sparse set of marker glyphs is overlaid at
regular time points.
}
