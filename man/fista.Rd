% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fista.R
\name{fista}
\alias{fista}
\title{Non-monotone Accelerated Proximal Gradient (nmAPG) solver — dispatcher.}
\usage{
fista(
  X,
  y,
  family,
  penalty,
  lambda_reg,
  fit_intercept = TRUE,
  rel_tol = 1e-04,
  step_size_init = 0.1,
  max_iter = 500L,
  eta_param = 0.8,
  delta_param = 1e-04,
  rho = 0.5,
  bb_growth_cap = 4,
  coef_init = NULL,
  intercept_init = NULL
)
}
\arguments{
\item{X}{Standardized design matrix.}

\item{y}{Response vector (length n).}

\item{family}{A pic family object (provides \code{name}).}

\item{penalty}{A pic penalty object (provides \code{name}, \code{params}).}

\item{lambda_reg}{Regularization parameter.}

\item{fit_intercept}{Whether to update an unpenalized intercept.}

\item{rel_tol}{Relative gradient-mapping tolerance.}

\item{step_size_init}{Initial step size.}

\item{max_iter}{Hard cap on outer iterations.}

\item{eta_param}{Memory weight of the non-monotone reference (in [0,1)).}

\item{delta_param}{Sufficient-descent constant in the line search.}

\item{rho}{Step-size reduction factor (< 1).}

\item{bb_growth_cap}{Maximum BB step growth over the previous step.}

\item{coef_init, intercept_init}{Optional warm starts.}
}
\value{
A list with \code{coef}, \code{intercept}, \code{info}.
}
\description{
Thin R wrapper around the C++ \code{fista_cpp} implementation. The actual
algorithm (Li & Lin, NeurIPS 2015) lives in \code{src/fista.cpp}; this
function only extracts the C++-facing arguments and routes the call.
}
\details{
Cox is routed to a dedicated C++ FISTA (\code{fista_cox_cpp}) because its
loss has no intercept and uses a 2-column \verb{(time, event)} response.
The build accepts only the six families and three penalties that have
a C++ implementation; the family / penalty registries throw on any
other input upstream.
}
\keyword{internal}
